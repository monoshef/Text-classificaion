Steps to run the code:

a. Install visual studio code
b. Install python 
c. import all the dependencies streamlit, river, pandas, matplotlib, altair, datetime(python ibraries used in the code)
d. now run the app using the command in the vs code terminal  streamlit run app_01.py
e. Make sure data.db file is not in the workspace(It gets created after running the code once)
